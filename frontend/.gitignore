# Dependencies
node_modules

# Build output (regenerated by `npm run build`; backend serves it in prod)
dist
dist-ssr

# Vite / tooling cache
.vite
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea
*.sw?

# Migration reference: a one-off dump of the old embedded page. It is redundant
# with the HTML still embedded in scripts/base_sensor_visual_server.py, so it is
# not tracked. Remove this line if you want it in version control.
legacy/
